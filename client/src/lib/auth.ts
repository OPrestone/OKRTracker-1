/**
 * Client-side auth utilities
 */

/**
 * This is just a stub function for client-side use
 * Actual password hashing is done on the server
 * We're not handling password hashing on the client
 */
export function hashPassword(password: string): string {
  // This is a client-side stub only
  // Password hashing is performed on the server side
  return password;
}